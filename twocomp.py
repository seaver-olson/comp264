import numpy as np

def multiply(num1,num2):
	w = (len(string(num2));
	product = np.zeros(w);
	for i in range(w):
		for e in 
def getDigit(num,i):
	x = string(num)
	x.reverse()
	return int(x[i])
				
		
		
